import styled from "styled-components";

export const Container = styled.section`
    ul {
        list-style-type: none;
        display: flex;
        flex-wrap: wrap;
        padding: 40px;
    }
`;
